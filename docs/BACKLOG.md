# Backlog de MADSuite

## High Priority

## Medium

- IA OpenAI (au-delà des suggestions billing existantes)

## Low

- Animations
- Migration stack cible Next.js/Prisma (long terme)

## Terminé ✅

- Harmonisation Branding (nom MADSuite uniforme partout dans le code, variables d'environnement, configurations et tests)
- Retrait `auth.controller.js` (WIP) et nettoyage dette technique
- Montage `securityBufferJob` dans le scheduler global
- Soumissions / Estimates (table DB + UI + conversion → facture)
- Stripe (Stripe webhooks avancés, statut invoice paid, gestion annulations complexes)
- PDF professionnels (branding MADSuite, taxes)
- Audit multi-tenant complet (toutes les tables sécurisées par RLS)
- Landing page + onboarding payant (Stripe Checkout Session avec essai de 14 jours)
- Rapports avancés / analytics dashboard (Intégration Recharts)
- Mobile punch (`/mobilepunch`)
- Calcul km (`/calculkm`)
- Dark mode global persisté
